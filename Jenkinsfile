// Powered by Infostretch 

timestamps {

node () {

	stage ('nginx_lua - Checkout') {
 	 checkout([$class: 'GitSCM', branches: [[name: '*/master']], doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], userRemoteConfigs: [[credentialsId: 'brogen1968', url: 'https://github.com/brogen1968/test']]]) 
	}
	stage ('nginx_lua - Build') {
 	
// Unable to convert a build step referring to "com.cloudbees.dockerpublish.DockerBuilder". Please verify and convert manually if required. 
	}
}
}